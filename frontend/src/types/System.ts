export interface SystemInfo {
  "heap.total": number;
  "heap.free": number;
  "heap.min_free": number;
  "storage.free": number;
  "storage.total": number;
  "uptime": number;
  "version": string;
}

